# Write imported articles once

OPMLark never automatically rewrites or deletes an article after successful ingestion. A reader may sync, annotate, or reorganize that Markdown without a later feed run destroying their work; changes published upstream can be collected only through an explicit future versioning workflow.
